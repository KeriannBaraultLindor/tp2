<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Mon Application</title>
  <style type="text/css">
    body{
      font-family: arial;
    }

    button{
      margin: 0 auto;
      padding: 0 auto;
      width: 173px;
    }

    ul{
      list-style: none;
      padding: 0px;
    }

    li{
      margin-bottom: 10px;
    }

    li:nth-child(odd){
      background-color: aliceblue;
    }

    .tableau {
      border-collapse: collapse;
      margin: 10px auto;
      width: 80%;
    }
    .tableau td {
      border: 1px dotted #aaa;
      padding: 10px;
    }
  </style>
</head>
<body>
  <form action="/adresse" method="POST">
    <button type="submit">Ajouter</button>
 
<!--
  <ul class="adresse">
  <% for(var i=0; i<adresse.length; i++) {%>
    <li class="client">
      <form action="modifier" method="POST">
        <input type="text" placeholder="prénom" name="prenom" value=<%= adresse[i].prenom %>>
        <input type="text" placeholder="nom" name="nom" value=<%= adresse[i].nom %>>
        <input type="text" placeholder="Telephone" name="telephone" value=<%= adresse[i].telephone %>>
        <input type="text" placeholder="Ville" name="ville" value=<%= adresse[i].ville %>>
        <input type="text" placeholder="Code Postal" name="codepostal" value=<%= adresse[i].codepostal %>>
        <input type="hidden" placeholder="id" name="_id" value=<%= adresse[i]._id %>>
        <button type="submit">Modifier</button>
      </form>
    </li><a <%= "href=/detruire/"+ adresse[i]._id %> >Détruire</a>
  <% } %>
  </ul>
  -->

  <table class="tableau" action="/adresse" method="POST">
      <tr>
        <td>Nom</td>
        <td>Prénom</td>
        <td>Téléphone</td>
        <td>_id</td>
        <td></td>
        <td></td>
      </tr>
    <% for(var i=0; i<adresse.length; i++) {%>
      <tr class="client">
        <td contenteditable="true"><%= adresse[i].prenom %></td>
        <td contenteditable="true"><%= adresse[i].nom %></td>
        <td contenteditable="true"><%= adresse[i].telephone %></td>
        <td contenteditable="true"><%= adresse[i]._id %></td>
        <td><a <%= "href=/modifier/"+ adresse[i]._id %> >Modifier</a></td>
        <td><a <%= "href=/detruire/"+ adresse[i]._id %> >Détruire</a></td>
      </tr>
    <% } %>
  </table>
  </form>

</body>
</html>